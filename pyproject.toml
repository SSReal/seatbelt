[project]
name = "seatbelt"
version = "0.1.0"
description = "Collection of harness frameworks for building agents with Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.5",
    "openai>=2.37.0",
    "pandas>=3.0.3",
]
