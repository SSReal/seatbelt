[project]
name = "seatbelt"
version = "0.1.0"
description = "Collection of harness frameworks for building agents with Python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["openai>=2.37.0"]
